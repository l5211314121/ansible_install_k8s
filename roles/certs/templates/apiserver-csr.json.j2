{
    "CN": "kubernetes",
    "hosts": [
        "10.0.0.1",
        "127.0.0.1",
        {% for host in groups['etcd'] %}"{{ host }}"{% if not loop.last %},{% endif %}{% endfor %}
        {% if apiserver_addr %}
            {% for adr in apiserver_addr.split(',') %}
                {{ addr }},
            {% endfor %}
        {% endif %}
        "kubernetes",
        "kubernetes.default",
        "kubernetes.default.svc",
        "kubernetes.default.svc.cluster",
        "kubernetes.default.svc.cluster.local"
    ],
    "key": {
        "algo": "rsa",
        "size": 2048
    },
    "names": [
        {
            "C": "CN",
            "L": "Beijing",
            "ST": "Beijing",
            "O": "k8s",
            "OU": "system"
        }
    ]
}