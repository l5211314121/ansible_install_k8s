[Unit]
Description=Kubenetes Kube-proxy
After=docker.service
Requires=docker.service

[Service]
EnvironmentFile={{workdir}}/kubernetes/cfg/kube-proxy
ExecStart=/usr/local/bin/kube-proxy $KUBE_PROXY_OPTS
Restart=on-failure
KillMode=process

[Install]
WantedBy=multi-user.target